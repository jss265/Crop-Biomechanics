This code is an isolated test.
The object of this code is to compile new code along side the production probe code.
This version of the production probe code is LIKELY OUTDATED!

Files superceeded by ISOLATED_JSS are outdated production code files. 
    - These files should have isolated edits (via functions, classes, header files, or imports)

Files superceeded by REFERENCE_ONLY are NavX2 Sanity Check files.
    - These files are for reference will making edits to new files.

Files superceeded by NEW_JSS are new files.
    - These files are to be added to the proudction code framework.

NOTE: Some files (header files) may not have a prefix. These files may be necessary for integration.